<nav class="navbar navbar-light bg-light">
  <div class="container-fluid px-4">
    <% if user_signed_in? %>
        <%= link_to current_user.username, root_path, class: "navbar-brand"  %>
        <%= link_to "Sign Out", destroy_user_session_path, 'data-turbo-method': :delete, class: 'btn btn-danger'%>
      <%else %>
        <%= link_to 'Home', root_path, class: "navbar-brand"  %>
      <div class="d-flex nav__container--session justify-content-between">

        <div class="dropdown">
          <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
            Sign In
          </button>
          <ul class="dropdown-menu " aria-labelledby="dropdownMenuButton1">
            <%= render 'session/sign_in' %>
          </ul>
        </div>

        <%= link_to 'Registration', new_user_registration_path, class: 'btn btn-primary'%>
      </div>
    <% end %>

  </div>
</nav>
