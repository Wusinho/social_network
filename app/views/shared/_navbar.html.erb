<nav class="navbar navbar-light bg-light">
  <div class="container-fluid px-4">
    <% if user_signed_in? %>
      <%= link_to current_user.email, root_path, class: "navbar-brand"  %>
      <div class="dropdown pl-5">
        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
          Log out
        </button>
        <ul class="dropdown-menu " aria-labelledby="dropdownMenuButton1">
          <li><%= link_to "Sign Out", destroy_user_session_path, 'data-turbo-method': :delete%></li>
        </ul>
      </div>
      <%else %>
      <%= link_to 'Home', root_path, class: "navbar-brand"  %>
      <div class="dropdown pl-5">
        <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">
          Sign In
        </button>
        <ul class="dropdown-menu " aria-labelledby="dropdownMenuButton1">
          <%= render 'session/sign_in' %>
          <li><%= link_to 'Registration', new_user_registration_path %></li>
        </ul>
      </div>
    <% end %>

  </div>
</nav>
