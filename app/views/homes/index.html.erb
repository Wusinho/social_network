
<% if user_signed_in? %>
  <h1>Hello <%= current_user.username %></h1>

  <%= render 'new_post', post: @post %>
<%end %>

<div class="container">
  <%= turbo_frame_tag 'posts' do %>
    <%= render @posts %>
  <% end %>
</div>
